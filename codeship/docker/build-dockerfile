# We're using the official OpenJDK image from the Dockerhub (https://hub.docker.com/_/openjdk/).
# Take a look at the available versions so you can specify the Java version you want to use.
FROM openjdk:8-jdk
